#include <iostream>
using namespace std;

int main() {
    int num = 10;

    // Use the increment operator to increase num by 1
    num++;

    // Use the decrement operator to decrease num by 1
    num--;

    // Output the final value of num
    cout << "The final value of num is: " << num << endl;

    return 0;
}

